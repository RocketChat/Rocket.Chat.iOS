codecov:
  branch: develop
  notify:
    require_ci_to_pass: yes

coverage:
  precision: 2 
  round: down
  range: 70...100

  status:
    project:
      enabled: yes
      threshold: 0.5
    patch: no
    changes: no

comment:
  layout: "header, diff, changes"
  behavior: default